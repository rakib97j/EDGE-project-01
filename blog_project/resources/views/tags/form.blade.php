<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Tag Form</h2></x-slot><div class="max-w-2xl mx-auto px-4 py-8"><form method="POST" action="{{ isset($tag) ? route('tags.update',$tag) : route('tags.store') }}" class="bg-white p-6 rounded shadow space-y-4">@csrf @if(isset($tag)) @method('PUT') @endif<input name="name" value="{{ old('name',$tag->name ?? '') }}" class="w-full border rounded p-2" placeholder="Name" required><input name="slug" value="{{ old('slug',$tag->slug ?? '') }}" class="w-full border rounded p-2" placeholder="Slug"><button class="bg-gray-900 text-white px-4 py-2 rounded">Save</button></form></div></x-app-layout>
